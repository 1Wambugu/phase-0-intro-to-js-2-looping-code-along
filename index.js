// Code your solutions in this file
//function writeCards(names, eventName) {
  //  const messages = [];
  
  //  for (let i = 0; i < names.length; i++) {
 //     const message = `Thank you, ${names[i]}, for attending the ${eventName}!`;
  //    messages.push(message);
 //   }
  
  //  return messages;
 // }
 // const names = ['Maureen', 'Martin', 'Nathaniel'];
//const eventName = 'birthday party';

//const result = writeCards(names, eventName);
//console.log(result);.//

function countDown(number) {
    while (number >= 0) {
      console.log(number);
      number--;
    }
  }
  countDown(10);